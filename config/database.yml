development:
  adapter: postgresql
  database: rails_json_cors_and_http_auth_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &test
  adapter: postgresql
  database: rails_json_cors_and_http_auth_test

production:
  adapter: postgresql
  database: rails_json_cors_and_http_auth

cucumber:
  <<: *test